% HDC-RWKV (nb12c) — FULL EXPANDED architecture (v3: vertical stack).
%
% Every block now occupies a full-width row and stacks vertically. Block 3
% and Block 4 are each split into two sub-panels so the internal detail
% has its own horizontal space. Aux A and Aux B are stacked below.
%
% Compile:  pdflatex architecture_hdc_rwkv_expanded.tex

\documentclass[tikz,border=14pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usetikzlibrary{positioning,arrows.meta,calc,fit,backgrounds,shapes.geometric,
                shapes.misc,decorations.pathreplacing,decorations.markings,matrix}

\newcommand{\bV}{\mathbf{V}}
\newcommand{\bP}{\mathbf{P}}
\newcommand{\bm}{\mathbf{m}}
\newcommand{\bh}{\mathbf{h}}
\newcommand{\bs}{\mathbf{s}}
\newcommand{\bz}{\mathbf{z}}
\newcommand{\bipset}{\{-1,+1\}}

\definecolor{cBipolar}{HTML}{2C5282}
\definecolor{cBipolarLite}{HTML}{EBF4FF}
\definecolor{cCont}{HTML}{B7791F}
\definecolor{cContLite}{HTML}{FFF8E6}
\definecolor{cOp}{HTML}{2D3748}
\definecolor{cSampler}{HTML}{6B46C1}
\definecolor{cSamplerLite}{HTML}{F3EBFF}
\definecolor{cAux}{HTML}{276749}
\definecolor{cAuxLite}{HTML}{F0FFF4}
\definecolor{cArrow}{HTML}{1A202C}
\definecolor{cPanelBg}{HTML}{FAFBFC}
\definecolor{cPanelBorder}{HTML}{CBD5E0}

% Panel width (uniform across all rows) — v3 wider for legibility
\def\panelW{22}   % in TikZ units (~= cm)

\tikzset{
  block/.style={draw=cPanelBorder, fill=cPanelBg, very thick, rounded corners=8pt,
                inner sep=10pt, minimum width=\panelW cm},
  % Sub-heading font bumped from \large to \LARGE per user request
  blockTitle/.style={font=\bfseries\LARGE, anchor=north west,
                     inner sep=8pt},
  cellPos/.style={draw=cBipolar!50, fill=cBipolarLite, font=\normalsize,
                  minimum width=9mm, minimum height=8mm, inner sep=0pt},
  cellNeg/.style={draw=cBipolar!50, fill=cBipolar!18, font=\normalsize,
                  minimum width=9mm, minimum height=8mm, inner sep=0pt},
  cellCont/.style={draw=cCont!60, fill=cContLite, font=\normalsize,
                   minimum width=13mm, minimum height=8mm, inner sep=0pt},
  cellPlain/.style={draw=black!30, fill=white, font=\normalsize,
                    minimum width=9mm, minimum height=8mm, inner sep=0pt},
  op/.style={draw=cOp, fill=white, thick, circle, inner sep=1.4pt,
             font=\normalsize, minimum size=8mm},
  flow/.style={-{Stealth[length=2.6mm]}, thick, draw=cArrow},
  flowSm/.style={-{Stealth[length=2mm]}, semithick, draw=cArrow!85},
  recur/.style={-{Stealth[length=2.6mm]}, thick, draw=cCont!80!black, dashed},
  lbl/.style={font=\small\itshape, text=black!65, align=center},
  arrowLbl/.style={font=\small, text=black!60, align=center,
                   fill=cPanelBg, inner sep=2pt},
  matlbl/.style={font=\small, text=cBipolar!85!black, align=center},
  idx/.style={font=\small\ttfamily, text=black!55},
  eqn/.style={font=\large},
  panel/.style={draw=cPanelBorder, fill=cPanelBg, very thick, rounded corners=8pt,
                inner sep=10pt, minimum width=\panelW cm},
  panelHeading/.style={font=\bfseries\LARGE, anchor=north west, inner sep=8pt},
  subHeading/.style={font=\normalsize\bfseries, anchor=north west},
}

\begin{document}
\begin{tikzpicture}[node distance=0mm]

% =====================================================================
% TITLE STRIP
% =====================================================================
\node[font=\LARGE\bfseries, anchor=north] (title) at (\panelW/2, 0)
   {HDC-RWKV (nb12c) --- Full Architecture Expansion};
\node[below=2pt of title, font=\small\itshape, text=black!60, anchor=north]
   {$V{=}256$, $d{=}256$, $L{=}1$, block size 16 \;$\bullet$\;
    16{,}434 deployment bytes \;$\bullet$\;
    every learnable parameter bipolar at inference};

% =====================================================================
% BLOCK 1 — Token lookup
% =====================================================================
\begin{scope}[shift={(0,-3)}]
  \node[block, minimum height=52mm, anchor=north west] (B1) at (0,0) {};
  \node[blockTitle] at (B1.north west)
    {\textcolor{cBipolar}{Block 1.}\;\;Token lookup --- bipolar embedding table};

  % centre the whole table horizontally
  \begin{scope}[shift={(4.5,-1.6)}]
    \node[idx, anchor=east] at (-0.3, 0) {col $i$:};
    \foreach \j in {0,1,2,3,4,5,6,7}
      \node[idx] at (\j*1.1, 0) {\j};
    \node[idx] at (8*0.9, 0) {$\cdots$};

    \foreach \row/\y/\pat in {0/-0.7/{+,-,+,-,+,-,+,-},
                              1/-1.35/{-,+,-,+,-,+,-,+},
                              2/-2.00/{+,+,-,-,+,+,-,-},
                              3/-2.65/{+,-,-,+,+,+,-,-}} {
      \node[idx, anchor=east] at (-0.3, \y) {row \row};
      \foreach \v [count=\i from 0] in \pat {
        \ifx\v+
          \node[cellPos] (r\row_\i) at (\i*1.1, \y) {$+$};
        \else
          \node[cellNeg] (r\row_\i) at (\i*1.1, \y) {$-$};
        \fi
      }
      \node[idx] at (8*0.9, \y) {$\cdots$};
    }
    \node[idx, anchor=east] at (-0.3, -3.25) {$\vdots$};
    \node[idx] at (3*0.9, -3.25) {$\vdots$};

    % brace
    \draw[decorate, decoration={brace, amplitude=5pt}]
      (8*0.9 + 0.6, -0.35) -- (8*0.9 + 0.6, -3.0);
    \node[matlbl, anchor=west] at (8*0.9 + 1.0, -1.7)
      {$\widetilde\bV \in \bipset^{V\times d}$\\[2pt] $V{=}256$ rows};

    % lookup pointer
    \draw[-{Stealth[length=2.4mm]}, thick, cCont!80!black]
      (-2.6, -1.35) -- (-1.05, -1.35);
    \node[font=\small\bfseries, text=cCont!80!black, anchor=east]
       at (-2.7, -1.35) {$x_t = 1$};
  \end{scope}
\end{scope}

% =====================================================================
% BLOCK 2 — Position binding
% =====================================================================
\begin{scope}[shift={(0,-9)}]
  \node[block, minimum height=62mm, anchor=north west] (B2) at (0,0) {};
  \node[blockTitle] at (B2.north west)
    {\textcolor{cBipolar}{Block 2.}\;\;Position binding $\rho^{t}$ (cyclic right-rotation)};

  \begin{scope}[shift={(4.5,-1.9)}]
    % before
    \node[lbl, anchor=east, align=right] at (-0.3, 0)
       {\textbf{before}\\$\widetilde\bV_{x_t}$};
    \foreach \v [count=\i from 0] in {-,-,+,+,-,+,-,+} {
      \ifx\v+
        \node[cellPos] (in\i) at (\i*1.15, 0) {$+$};
      \else
        \node[cellNeg] (in\i) at (\i*1.15, 0) {$-$};
      \fi
      \node[idx, above=1pt of in\i] {\i};
    }

    % rotation arrow with clear label
    \draw[-{Stealth[length=2.8mm]}, ultra thick, cCont!80!black]
      (3.5*0.95, -0.5) -- (3.5*0.95, -1.35);
    \node[font=\small\bfseries, text=cCont!80!black, anchor=west]
      at (3.5*0.95 + 0.25, -0.9) {$\rho^{2}$};

    % after
    \node[lbl, anchor=east, align=right] at (-0.3, -1.85)
      {\textbf{after}\\$\rho^{2}(\widetilde\bV_{x_t})$};
    \foreach \v [count=\j from 0] in {-,+,-,-,+,+,-,+} {
      \ifx\v+
        \node[cellPos] (out\j) at (\j*1.15, -1.85) {$+$};
      \else
        \node[cellNeg] (out\j) at (\j*1.15, -1.85) {$-$};
      \fi
      \node[idx, below=1pt of out\j] {\j};
    }
  \end{scope}

  % description and formula stacked cleanly below the cells
  \node[lbl, align=center, text width=140mm, anchor=north]
    at (\panelW/2, -4.5)
    {Cell at index $j$ takes value from index $(j - t) \bmod d$. \;
     Different positions produce orthogonal versions of the same token.};
  \node[matlbl, anchor=north]
    at (\panelW/2, -5.2)
    {$\rho^{t}(\bh)_j = \bh_{(j-t)\bmod d}$ \quad\emph{(parameter-free, preserves $\bipset^{d}$)}};
\end{scope}

% =====================================================================
% BLOCK 3a — Recurrence: per-channel detail (own row)
% =====================================================================
\begin{scope}[shift={(0,-16.5)}]
  \node[block, minimum height=78mm, anchor=north west] (B3a) at (0,0) {};
  \node[blockTitle] at (B3a.north west)
    {\textcolor{cCont!75!black}{Block 3a.}\;\;Recurrence --- per-channel detail at one timestep $t$};

  \begin{scope}[shift={(4.0,-2.2)}]
    % decay
    \node[lbl, anchor=east, align=right] at (-0.3, 0)
       {decay mask\\$\widetilde\bm$};
    \foreach \v [count=\i from 0] in {+,-,+,+,-,+,-,-} {
      \ifx\v+
        \node[cellPos] (m\i) at (\i*1.1, 0) {$+$};
      \else
        \node[cellNeg] (m\i) at (\i*1.1, 0) {$-$};
      \fi
    }

    % prev state
    \node[lbl, anchor=east, align=right] at (-0.3, -1.2)
       {prev state\\$\bs_{t-1}$};
    \foreach \v [count=\i from 0] in {0.31,-0.84,0.55,0.12,-0.71,0.92,-0.43,0.05} {
      \node[cellCont] (s\i) at (\i*1.1, -1.2) {\v};
    }

    % rotated input
    \node[lbl, anchor=east, align=right] at (-0.3, -2.4)
       {rotated input\\$\bh_t$};
    \foreach \v [count=\i from 0] in {+,+,-,+,+,-,-,+} {
      \ifx\v+
        \node[cellPos] (h\i) at (\i*1.1, -2.4) {$+$};
      \else
        \node[cellNeg] (h\i) at (\i*1.1, -2.4) {$-$};
      \fi
    }

    % new state
    \node[lbl, anchor=east, align=right] at (-0.3, -4.2)
       {new state\\$\bs_t$};
    \foreach \v [count=\i from 0] in {0.86,0.95,-0.42,0.81,0.94,-0.08,-0.52,0.74} {
      \node[cellCont] (sn\i) at (\i*1.1, -4.2) {\v};
    }

    % wiring for channel i=2 in a dedicated column between prev-state and new-state rows
    \draw[->, thick, cCont] (m2.south) -- ++(0,-0.35);
    \draw[->, thick, cCont] (s2.east) -- ++(0.15,0) |- (1.6*0.9, -0.9);
    \node[op, fill=cContLite] (mul2) at (1.6*0.9, -1.85) {$\odot$};
    \draw[->, thick, cCont] (1.6*0.9, -0.9) -- (mul2.north);
    \node[op, fill=cContLite] (add2) at (1.6*0.9, -3.1) {$+$};
    \draw[->, thick, cCont] (mul2.south) -- (add2.north);
    \draw[->, thick, cCont] (h2.east) -- ++(0.1,0) |- (add2.east);
    \draw[->, thick, cCont] (add2.south) -- (sn2.north);
    \node[arrowLbl, right=2pt] at (1.6*0.9, -3.7) {$\tanh$};

    % worked-channel note pinned to the right of the cell grid
    \node[matlbl, align=left, anchor=west, text width=40mm]
       at (8*0.9 + 0.6, -2.1)
      {\textbf{channel $i{=}2$:}\\[2pt]
       $\widetilde m_2 \cdot s_{t-1,2} + h_{t,2}$\\
       $= (+1)(0.55) + (-1) = -0.45$\\
       $\tanh(-0.45) \approx -0.42$};
  \end{scope}

  % equation banner along the bottom (its own row of space)
  \node[eqn, anchor=north] at (\panelW/2, -8.3)
     {$\bs_t \;=\; \tanh\!\bigl(\,\widetilde\bm \odot \bs_{t-1} \;+\; \bh_t\,\bigr)$
      \quad\emph{(one channel at a time)}};
\end{scope}

% =====================================================================
% BLOCK 3b — Recurrence: unrolled across time (own row)
% =====================================================================
\begin{scope}[shift={(0,-26)}]
  \node[block, minimum height=68mm, anchor=north west] (B3b) at (0,0) {};
  \node[blockTitle] at (B3b.north west)
    {\textcolor{cCont!75!black}{Block 3b.}\;\;Recurrence --- unrolled across time};

  % timeline in the middle of the panel
  \begin{scope}[shift={(2.0,-2.5)}]
    \node[cellCont, minimum width=16mm] (st0) at (0,   0) {$\bs_{-1}{=}\mathbf{0}$};
    \node[op] (rec0) at (2.0, 0) {$\mathcal{R}$};
    \node[cellCont, minimum width=12mm] (st1) at (3.6, 0) {$\bs_0$};
    \node[op] (rec1) at (5.2, 0) {$\mathcal{R}$};
    \node[cellCont, minimum width=12mm] (st2) at (6.8, 0) {$\bs_1$};
    \node[op] (rec2) at (8.4, 0) {$\mathcal{R}$};
    \node[cellCont, minimum width=12mm] (st3) at (10.0,0) {$\bs_2$};
    \node[op] (rec3) at (11.6,0) {$\mathcal{R}$};
    \node[cellCont, minimum width=12mm] (st4) at (13.2,0) {$\bs_3$};
    \node[font=\Large, anchor=west] at (13.9, 0) {$\cdots$};
    \foreach \a/\b in {st0/rec0, rec0/st1, st1/rec1, rec1/st2,
                       st2/rec2, rec2/st3, st3/rec3, rec3/st4}
      \draw[flow] (\a) -- (\b);

    % per-step inputs sitting well below the timeline (no overlap)
    \foreach \i/\rname in {0/rec0, 1/rec1, 2/rec2, 3/rec3} {
      \node[cellPos, minimum width=12mm]
         (hin\i) at ([yshift=-2.0cm]\rname) {$\bh_{\i}$};
      \draw[flowSm] (hin\i.north) -- (\rname.south);
    }
    \node[lbl, align=center, text width=140mm, anchor=north]
       at (6.8, -3.4)
       {position-tagged inputs from Block 2 (one bipolar vector per step)};
  \end{scope}

  % formula on its own row at the bottom
  \node[lbl, align=center, text width=160mm, anchor=north] at (\panelW/2, -6.4)
     {$\mathcal{R}(\bs_{t-1}, \bh_t) \;=\; \tanh\!\bigl(\widetilde\bm \odot \bs_{t-1} + \bh_t\bigr)$
      \quad\quad
      $O(d)$ work per step. \;No attention. \;No key--value cache.};
\end{scope}

% =====================================================================
% BLOCK 4a — Prototype output: dot products (own row)
% =====================================================================
\begin{scope}[shift={(0,-35)}]
  \node[block, minimum height=80mm, anchor=north west] (B4a) at (0,0) {};
  \node[blockTitle] at (B4a.north west)
    {\textcolor{cSampler!75!black}{Block 4a.}\;\;Prototype-similarity output --- one dot product per vocabulary item};

  \begin{scope}[shift={(4.0,-2.2)}]
    % current state on top
    \node[lbl, anchor=east, align=right] at (-0.3, 0)
       {current state\\$\bs_t \in (-1,+1)^{d}$};
    \foreach \v [count=\i from 0] in {0.86,0.95,-0.42,0.81,0.94,-0.08,-0.52,0.74} {
      \node[cellCont] (ss\i) at (\i*1.1, 0) {\v};
    }
    \node[idx] at (8*0.9, 0) {$\cdots$};

    % prototype rows
    \foreach \row/\y/\pat in {0/-1.2/{+,+,-,-,+,+,-,-},
                              1/-1.9/{-,+,+,-,-,+,+,-},
                              2/-2.6/{+,-,+,-,+,-,+,-},
                              v/-3.5/{?,?,?,?,?,?,?,?},
                              V/-4.3/{-,-,+,+,-,+,-,+}} {
      \node[lbl, anchor=east] at (-0.3, \y) {row \row};
      \foreach \v [count=\i from 0] in \pat {
        \ifx\v+
          \node[cellPos] (p\row_\i) at (\i*1.1, \y) {$+$};
        \else\ifx\v-
          \node[cellNeg] (p\row_\i) at (\i*1.1, \y) {$-$};
        \else
          \node[cellPlain] (p\row_\i) at (\i*1.1, \y) {?};
        \fi\fi
      }
      \node[idx] at (8*0.9, \y) {$\cdots$};
    }

    % brace on prototype matrix
    \draw[decorate, decoration={brace, amplitude=5pt}]
      (8*0.9 + 0.6, -0.85) -- (8*0.9 + 0.6, -4.65);
    \node[matlbl, anchor=west] at (8*0.9 + 1.0, -2.7)
      {$\widetilde\bP \in \bipset^{V\times d}$\\$V{=}256$ prototypes};

    % dot products in a right column
    \foreach \row/\y/\val in {0/-1.2/{+0.42},
                              1/-1.9/{-1.21},
                              2/-2.6/{+2.96},
                              v/-3.5/{$z_{t,v}$},
                              V/-4.3/{-0.18}} {
      \node[op, fill=cSamplerLite] (dot\row) at (8*0.9 + 3.3, \y)
        {\scriptsize$\langle\!\cdot,\!\cdot\!\rangle$};
      \node[cellCont, minimum width=14mm] (lg\row) at (8*0.9 + 4.8, \y) {\val};
      \draw[flow] (dot\row) -- (lg\row);
    }
    \node[lbl, anchor=west, text=black!70] at (8*0.9 + 4.1, -0.5) {raw logits};
  \end{scope}
\end{scope}

% =====================================================================
% BLOCK 4b — Softmax → sampler → next token (own row)
% =====================================================================
\begin{scope}[shift={(0,-45)}]
  \node[block, minimum height=48mm, anchor=north west] (B4b) at (0,0) {};
  \node[blockTitle] at (B4b.north west)
    {\textcolor{cSampler!75!black}{Block 4b.}\;\;Temperature scaling, softmax, top-$k$ sampling, autoregressive feedback};

  \begin{scope}[shift={(2.5,-2.5)}]
    % raw logits vector node
    \node[cellCont, minimum width=22mm, minimum height=8mm]
      (rawZ) at (0, 0) {raw logits $\bz_t \in \mathbb{R}^{V}$};

    % divide by tau
    \node[op, fill=cSamplerLite, minimum size=10mm] (divT) at (3.4, 0) {$/\tau$};

    % softmax box
    \node[draw=cSampler, fill=cSamplerLite, thick, rounded corners=3pt,
          minimum width=22mm, minimum height=10mm, font=\small]
          (sm) at (5.9, 0) {softmax};

    % top-k box
    \node[draw=cSampler, fill=cSamplerLite, thick, rounded corners=3pt,
          minimum width=26mm, minimum height=10mm, font=\small]
          (tk) at (9.2, 0) {top-$k$ sample};

    % next token
    \node[draw=black!70, thick, rounded corners=2pt, minimum width=22mm,
          minimum height=10mm, font=\small\ttfamily]
          (nxt) at (12.8, 0) {next $x_{t+1}$};

    \draw[flow] (rawZ) -- (divT);
    \draw[flow] (divT) -- (sm);
    \draw[flow] (sm) -- (tk);
    \draw[flow] (tk) -- (nxt);

    % labels sit BELOW nodes so they don't collide
    \node[lbl, below=1pt of divT] {$\tau = e^{\log\tau}$};
    \node[arrowLbl, above=1pt] at ($(sm)!0.5!(tk)+(0,0.5)$) {probabilities};

    % autoregressive feedback (loops back down and left)
    \draw[recur] (nxt.south) -- ++(0,-1.5) -| (-2.5,0);
    \node[lbl, align=center, anchor=north, text width=120mm]
       at (7, -2.0)
       {\textbf{autoregressive feedback:} next token becomes input at step $t{+}1$
        (re-enters Block 1)};
  \end{scope}
\end{scope}

% =====================================================================
% AUX A — STE forward/backward (own row)
% =====================================================================
\begin{scope}[shift={(0,-51)}]
  \node[panel, minimum height=65mm, anchor=north west] (AA) at (0,0) {};
  \node[panelHeading] at (AA.north west)
    {\textcolor{cAux}{Aux A.}\;\;Straight-Through Estimator ($\widetilde{\mathrm{sign}}$)};

  % left half: forward
  \begin{scope}[shift={(3.5,-3.0)}]
    \node[subHeading, anchor=south west] at (-2.6, 1.6)
       {Forward: $y = \mathrm{sign}(x)$};
    \draw[->] (-2.3,0) -- (2.3,0) node[right, font=\scriptsize] {$x$};
    \draw[->] (0,-1.3) -- (0,1.5) node[above, font=\scriptsize] {$y$};
    \draw[thick, cBipolar] (-2.2,-1) -- (-0.03,-1);
    \draw[thick, cBipolar] (0.03, 1) -- ( 2.2, 1);
    \draw[thick, cBipolar, dotted] (0,-1) -- (0,1);
    \node[font=\scriptsize] at (1.5,-0.3) {$+1$};
    \node[font=\scriptsize] at (-1.5,0.3) {$-1$};
  \end{scope}

  % right half: backward
  \begin{scope}[shift={(12,-3.0)}]
    \node[subHeading, anchor=south west] at (-2.6, 1.6)
       {Backward: $\partial y/\partial x = \mathbb{1}[\,|x|\le 1\,]$};
    \draw[->] (-2.3,0) -- (2.3,0) node[right, font=\scriptsize] {$x$};
    \draw[->] (0,-0.4) -- (0,1.6) node[above, font=\scriptsize] {$\partial y/\partial x$};
    \draw[thick, cAux] (-2.2,0) -- (-1.0,0);
    \draw[thick, cAux] (-1.0,1) -- (1.0, 1);
    \draw[thick, cAux] (1.0, 0) -- (2.2, 0);
    \draw[dashed, cAux!50] (-1.0,0) -- (-1.0,1);
    \draw[dashed, cAux!50] ( 1.0,0) -- ( 1.0,1);
    \node[font=\scriptsize, anchor=south] at (0,1.05) {$= 1$};
    \node[font=\scriptsize] at (-1.6,-0.3) {$0$};
    \node[font=\scriptsize] at ( 1.6,-0.3) {$0$};
  \end{scope}

  \node[lbl, align=center, text width=160mm, anchor=south]
    at (\panelW/2, -6.0)
    {Forward is hard binarisation; backward is identity on the active strip
     $[-1,1]$, zero outside. The optimiser updates the continuous shadow
     $\bV$ while the recurrence always sees $\widetilde\bV \in \bipset^{V\times d}$.};
\end{scope}

% =====================================================================
% AUX B — XOR + popcount (own row, stacked below Aux A)
% =====================================================================
\begin{scope}[shift={(0,-59)}]
  \node[panel, minimum height=68mm, anchor=north west] (AB) at (0,0) {};
  \node[panelHeading] at (AB.north west)
    {\textcolor{cAux}{Aux B.}\;\;Deployment equivalent --- XOR + popcount};

  \begin{scope}[shift={(1.5,-2.0)}]
    \node[font=\small, anchor=west] at (0, 0)
      {Encode $-1\mapsto 0$, $+1\mapsto 1$. Then for $a,b\in\bipset^d$:};
    \node[eqn, anchor=west] at (0.5, -0.85)
      {$\langle a,b\rangle \;=\; d - 2\cdot\mathrm{popcount}(\bar a \oplus \bar b).$};

    % worked example rows
    \begin{scope}[shift={(0.5,-2.4)}]
      \node[lbl, anchor=east] at (-0.15, 0) {$\bar a$:};
      \foreach \v [count=\i from 0] in {1,0,1,1,0,1,0,1}
        \node[cellPlain, font=\scriptsize\ttfamily] at (\i*0.75, 0) {\v};

      \node[lbl, anchor=east] at (-0.15, -0.7) {$\bar b$:};
      \foreach \v [count=\i from 0] in {1,1,1,0,0,1,1,0}
        \node[cellPlain, font=\scriptsize\ttfamily] at (\i*0.75, -0.7) {\v};

      \node[lbl, anchor=east] at (-0.15, -1.6) {$\bar a\oplus\bar b$:};
      \foreach \v [count=\i from 0] in {0,1,0,1,0,0,1,1} {
        \ifnum\v=1
          \node[cellPlain, fill=cAux!25, font=\scriptsize\ttfamily] at (\i*0.75, -1.6) {\v};
        \else
          \node[cellPlain, font=\scriptsize\ttfamily] at (\i*0.75, -1.6) {\v};
        \fi
      }

      \node[font=\small, anchor=west, align=left] at (8*0.75 + 1.0, -0.8)
        {popcount $= 4$\\
         $d - 2\cdot 4 = 0$\\
         \emph{(orthogonal!)}};
    \end{scope}
  \end{scope}

  \node[lbl, align=center, text width=160mm, anchor=south]
    at (\panelW/2, -6.3)
    {The 6502 inner loop: XOR two $d/8$-byte rows, popcount the result,
     subtract $2\cdot$popcount from $d$. \;
     No multiplications, no floating point, no NaN reachable.\;
     For $d{=}256$: 32 bytes XORed + one popcount table lookup per byte.};
\end{scope}

\end{tikzpicture}
\end{document}
